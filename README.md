# EventeEase-Ticketing-Booking-Platform
A full-stack event booking system built with React (SPA) and a Node.js/Express REST API. Data is persisted in SQLite with structured CRUD endpoints. Leaflet JS provides interactive geolocation mapping. The platform supports event listings, detail retrieval, and secure ticket booking workflows.
